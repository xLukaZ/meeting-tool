# Ensure shell scripts always have LF line endings on checkout
# so they run correctly on the Linux VPS
*.sh    text eol=lf
*.conf  text eol=lf
*.service text eol=lf

# Everything else: auto-detect
*       text=auto
